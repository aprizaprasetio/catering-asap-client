import React from 'react'
import { LinearProgress } from '@mui/material'

const LoadingFull = () => <LinearProgress sx={{ position: 'fixed', left: 0, bottom: 0, width: '100%' }} />

export default LoadingFull